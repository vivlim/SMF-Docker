#!/bin/bash

# Username of the mariadb user to back up as
SMF_DB_USERNAME="smfuser"

# Password of the mariadb user to back up as
SMF_DB_PASSWORD="smfpass"

# Where to save the backups
BACKUP_PATH="$PWD/backups"